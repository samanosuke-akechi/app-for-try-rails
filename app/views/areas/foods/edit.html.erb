<%= form_with model: @food_form, url: area_foods_path(@area), method: :patch do |f| %>
  <%= render 'form', f: f %>
<% end %>

<% breadcrumb :area_food_edit, @area %>
<%= render 'shared/breadcrumb' %>
